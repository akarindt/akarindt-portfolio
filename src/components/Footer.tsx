export default function Footer() {
    return (
        <footer className="w-full py-8 bg-white dark:bg-gray-900 border-t border-gray-200 dark:border-gray-800">
            <div className="container mx-auto px-4 flex justify-center">
                <p className="text-center text-sm text-gray-600 dark:text-gray-400 font-light italic">
                    "You won't get anywhere if you only try things you can already do"
                </p>
            </div>
        </footer>
    );
}
